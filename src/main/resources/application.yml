spring:
  batch:
    initialize-schema: "always"
  datasource:
    url: "jdbc:mysql://localhost:3306/batch?useSSL=false&allowPublicKeyRetrieval=true"
    username: "batch"
    password: "batch"
    initialization-mode: "always"

inputFile: "/batch/in.csv"
outputFile: "D:/out.csv"
